name: br_ppo_crypto_v7 paper trading

on:
  workflow_dispatch:
  schedule:
    - cron: "0 09 * * 1-5"
    - cron: "0 14 * * 1-5"

permissions:
  contents: write

jobs:
  paper-trade:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run paper-trading logger
        env:
          ALPACA_CRYPTO_V7_KEY_ID: ${{ secrets.ALPACA_CRYPTO_V7_KEY_ID }}
          ALPACA_CRYPTO_V7_SECRET_KEY: ${{ secrets.ALPACA_CRYPTO_V7_SECRET_KEY }}
          ALPACA_BASE_URL: ${{ vars.ALPACA_BASE_URL || 'https://paper-api.alpaca.markets' }}
          SUBMIT_ORDERS: ${{ vars.SUBMIT_ORDERS || 'false' }}
          CANCEL_OPEN_ORDERS: ${{ vars.CANCEL_OPEN_ORDERS || 'false' }}
          ALLOCATION_MODE: ${{ vars.ALLOCATION_MODE || 'ppo' }}
          DEFAULT_ACTION: ${{ vars.DEFAULT_ACTION || 'v7_symbolic_optuna_00__long_top' }}
          MIN_ORDER_NOTIONAL: ${{ vars.MIN_ORDER_NOTIONAL || '25' }}
          REBALANCE_EVERY_DAYS: ${{ vars.REBALANCE_EVERY_DAYS || '10' }}
          FORCE_REBALANCE: ${{ vars.FORCE_REBALANCE || 'false' }}
        run: python scripts/paper_trade.py

      - name: Commit dashboard logs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add logs
          if git diff --cached --quiet; then
            echo "No log changes to commit."
          else
            git commit -m "Update br_ppo_crypto_v7 dashboard logs"
            git push
          fi
